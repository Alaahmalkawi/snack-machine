Please answer the following questions with no more than 2-3 lines

1- What are Mutation Observers in JavaScript?
Answer:  The mutation observer allow the developer to monitor the changes in html document.

2- Explain 3 different CSS selectors.
Answer:
a. node type selector: to find the elements by html node name.
b. class selector: to find all the elements that have the specfic css class.
c. Id selector: to find a single element has the specific Id.

3- What is the difference between SessionStorage and LocalStorage in HTML5?
Answer:
Session storage: the data will be deleted when the user closed the the browser.
Local storage: the data is staying in the storage until the application delete it.
